{
  "name": "anchord",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun --filter backend dev",
    "start": "bun --filter backend start",
    "test": "bun --filter backend test",
    "test:integration": "bun --filter backend test:integration",
    "typecheck": "bun --filter backend typecheck",
    "db:generate": "bun --filter backend db:generate",
    "db:migrate": "bun --filter backend db:migrate"
  }
}
